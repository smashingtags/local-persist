# Local Persist Docker Volume Plugin Configuration
# Copy this file to .env and customize the values for your environment

# Plugin Data Path - Where plugin state is stored on host
PLUGIN_DATA_PATH=/var/lib/docker/plugin-data

# Volume Mount Points - Customize these paths for your persistent volumes
DATA_MOUNTPOINT=/data/local-persist/shared
LOGS_MOUNTPOINT=/data/local-persist/logs
DB_MOUNTPOINT=/data/local-persist/database

# Logging Configuration
LOG_LEVEL=info

# Development Settings (uncomment for development)
# COMPOSE_FILE=docker-compose.yml:docker-compose.dev.yml
# BUILD_TARGET=dev